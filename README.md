# Terraform Examples

This repo contains multiple terraform examples. Each example has its own README, which has required information for the example.

## Requirements

 - You need to install [Terraform](https://learn.hashicorp.com/terraform/getting-started/install.html) on the machine where you want to test out these examples.

To try any example, you can clone this repo & run following command inside that specific example directory:

	$ terraform init

